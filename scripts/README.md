# Active Scripts

This directory has been reset for the rewrite.

Legacy packaging scripts were moved to `legacy/scripts-legacy/`.

Active operational entry points now live at the repository root:

- `start.sh`
- `start.bat`
- `stop.sh`
- `stop.bat`
- `deploy.sh`
